/**
 * Tailwind CSS v4: design tokens and plugins live in `src/app/globals.css`
 * (`@import "tailwindcss"`, `@theme`, `@plugin`, etc.).
 * This file exists so `components.json` → `tailwind.config` resolves for the shadcn CLI.
 */
import type { Config } from "tailwindcss";

export default {} satisfies Config;
